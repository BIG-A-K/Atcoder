#include <bits/stdc++.h>
#define ll long long
#define test(i) cout<<"test: "<<i<<endl
#define rep(i,n) for(int i = 0;i < (n); i++)
using namespace std;

int main(){
    int N,M;
    cin>>N>>M;
    vector<int> A(M);
    rep(i,M){
        cin>>A[i];
    }
    vector<set<long long>> s(N);
    rep(i,N){
        
    }
}

#include <bits/stdc++.h>
#define pi acos(-1)
#define ll long long
#define test(i) cout<<"test: "<<i<<endl
using namespace std;

int main(){
    int i,j;    
    int N,M;
    cin>>N>>M;
    vector<ll int> a(N);
    vector<vector<ll int>> aSET(N,vector<ll int>(N));
    for(i=0;i<N;i++){
        cin>>a[i];
        
    }
}
